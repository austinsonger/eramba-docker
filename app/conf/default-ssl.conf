<IfModule mod_ssl.c>
	<VirtualHost *:8080> 
		ServerName grc.formassembly.com
		ServerAlias www.grc.formassembly.com
		DocumentRoot /var/www/html
		Redirect permanent / https://grc.formassembly.com/
	</VirtualHost>

	<VirtualHost *:8443>
		ServerName grc.formassembly.com
		ServerAdmin austin@formassembly.com
		DocumentRoot /var/www/html
		LogLevel info ssl:debug
		ErrorLog ${APACHE_LOG_DIR}/error.log
		CustomLog ${APACHE_LOG_DIR}/access.log combined

		<Directory /var/www/html/>
			Options +Indexes
			AllowOverride All
			Options FollowSymLinks 
			Options -MultiViews
			allow from all
			deny from all
		</Directory>

		SSLEngine on

		SSLCertificateFile	/certs/grc.formassembly.com.cert
		SSLCertificateKeyFile /certs/grc.formassembly.com.key

		<FilesMatch "\.(cgi|shtml|phtml|php)$">
				SSLOptions +StdEnvVars
		</FilesMatch>
		<Directory /usr/lib/cgi-bin>
				SSLOptions +StdEnvVars
		</Directory>

	</VirtualHost>
</IfModule>
# vim: syntax=apache ts=4 sw=4 sts=4 sr noet